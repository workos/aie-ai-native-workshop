node_modules/
# This project uses Bun (bun.lock); npm's lockfile is not used.
package-lock.json
board/public/
board/.wrangler/
.dev.vars

# Coach check-in local state (per-attendee, never committed)
.aie-coach-state.json
.aie-coach-outbox/

# OS / editor cruft
.DS_Store
*.log
